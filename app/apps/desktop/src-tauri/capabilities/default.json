{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window. Dev IPC is limited to the local Next server that devUrl loads.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://localhost:3000/*", "http://127.0.0.1:3000/*"]
  },
  "permissions": ["core:default"]
}
